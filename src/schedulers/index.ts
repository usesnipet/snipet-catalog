import type { Schedule } from "./schedule.types.js";

export type { Schedule, ScheduleContext } from "./schedule.types.js";

/**
 * All registered schedules. Add new schedule files here.
 */
export const schedules: Schedule[] = [];
